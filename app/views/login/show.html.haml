#login_form
  #login_text 
    = t(:login_text)
  = render :partial => 'shared/login_notice'
  - form_tag login_path do
    = text_field_tag 'code', params[:code]
    = submit_tag '', :class => 'ok'
    .clear